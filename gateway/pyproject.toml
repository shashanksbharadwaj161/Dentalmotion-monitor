[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dentalmotion-gateway"
version = "0.2.7"
requires-python = ">=3.9"
dependencies = [
    "Flask==3.0.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["dentalmotion_gateway*"]
